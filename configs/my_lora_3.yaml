lora:
  lora_rank: 16
  lora_alpha: 32.0
  lora_dropout: 0.05
  target_modules: wq,wk,wv,wo
  save_mode: EMA + Full両方
  attention_backend: sdpa
  use_early_stopping: false
  es_patience: 3
  es_min_delta: 0.01
  use_ema: true
  ema_decay: 0.9999
  batch_size: 8
  grad_accum: 1
  lr: 0.0015
  optimizer: adamw
  lr_scheduler: cosine
  warmup_steps: 0
  max_steps: 3000
  save_every: 50
  log_every: 10
  valid_ratio: 0.1
  valid_every: 100
  wandb_enabled: true
  wandb_project: ''
  wandb_run_name: ''
  seed: 42
